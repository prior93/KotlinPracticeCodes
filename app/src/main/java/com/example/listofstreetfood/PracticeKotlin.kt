package com.example.listofstreetfood

fun main() {
    /*
    // ---------------------------- While Loop ----------------------------

    var feltTemp = "Cold"
    var roomTemp = 10

    while (feltTemp == "Cold") {
        roomTemp++
        if (roomTemp >= 20) {
            feltTemp = "Comfy"
            println("Its comfortable Now")

        }
    }

    // ------------------------------------- For ------------------------------------

    for(i in 1..10){
        println("For loop is $i")   //->  For loop is 1,  Foor loop is 2
    ---------------------------- ---------------------------- ----------------------------
       for(i in 1 until 10)
           print(i)
    ---------------------------- ---------------------------- ----------------------------
    for (i in 10 downTo  1)
        print(i)
    ---------------------------- ---------------------------- ----------------------------

    for (i in 10 downTo 1 step 2)   //ANs 108642
        print(i)

   // **  --------METHOD IS FUNCTION WITHIN CLASS ---------------------- **
    // ------------------------------------- Function------------------------------------
    var result = addup(5,6)
    print("The Result is $result")

    fun addup(a:Int,b:Int) : Int{   // Parameter
        return a + b  //The Result is 11
    }



    // ------------------------------------- NULLABLE TYPE------------------------------------
var name:String = "Prior"
 var nullableName: String?  = "Prior"
//   nullableName = null

    var len = name.length
    var len2 = nullableName?.length// its same  like using if statement use just ?
    nullableName?.let { println(it.length) }



    // -----ELVIS OPERATOR ?: ---------------------- NULLABLE TYPE----------------------------
    var nullableName: String?  = "Prior"
//    nullableName = null

    val name = nullableName ?:"Guest"  // ?   safe call operator
    println(name)

    println(nullableName !!.toUpperCase())
    }
     */

}